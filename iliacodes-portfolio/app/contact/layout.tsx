
export const metadata = {
  title: 'IliaCodes',
  description: 'IliaCodes Portfolio, Vancouver based Blockchain developer and auditor. Contact email.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>;
}
