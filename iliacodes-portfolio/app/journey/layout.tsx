
export const metadata = {
  title: 'IliaCodes',
  description: 'IliaCodes Portfolio, Journey into Blockchain Development.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>;
}
